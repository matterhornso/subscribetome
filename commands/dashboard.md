---
description: Open the subscribetome dashboard to view and manage API keys and subscriptions
---

Run `stm dashboard` in a Bash command. It starts the subscribetome daemon (if
it is not already running), prints a `127.0.0.1` dashboard URL, and opens it in
the browser.

Report the dashboard URL to the user. Do not display or echo any API key value.
