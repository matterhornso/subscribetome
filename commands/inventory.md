---
description: Show the subscribetome inventory — API keys, subscriptions, and monthly spend
---

Run `stm list` in a Bash command and show the user the output: their API key
placeholders, subscriptions, and total monthly spend.

Never run `stm resolve` or otherwise display a key's plaintext value.
